// Import the mathUtils.js module with the exported functions.

import { square, average } from './mathUtils.js';

// Run both functions and log their output to the console
console.log(square(3))
console.log(average(3, 4 , 9))